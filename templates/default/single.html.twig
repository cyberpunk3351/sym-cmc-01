{% extends 'base.html.twig' %}

{% block title %}My First Admin!{% endblock %}

{% block body %}
    <div class="container">
        <h2>{{ post.title }}</h2>
        <div>Title1: {{ post.content|replace({' ':''}) }}</div>
    </div>



{% endblock %}