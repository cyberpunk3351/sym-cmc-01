{% extends 'base.html.twig' %}

{% block title %}My First Admin!{% endblock %}

{% block body %}
    <div class="container">
        <h3>Tag {{ tag }}</h3>
        <h3>Tag {{ tag.id }}</h3>
        {% for posts in tag.posts %}
            <p>{{ posts.title }}</p>
        {% endfor %}
    </div>
{% endblock %}